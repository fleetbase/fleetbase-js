'use strict';

module.exports = function (environment) {
    return {
        modulePrefix: 'fleetbase-sdk-ember',
        environment,
        rootURL: '/',
        locationType: 'history',
        EmberENV: { EXTEND_PROTOTYPES: false },
        APP: {},
    };
};
